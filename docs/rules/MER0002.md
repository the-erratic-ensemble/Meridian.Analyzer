# MER0002

## Summary

`MER0002` flags broad nested `try/catch` fallback flow inside another `try` block.

## Intent

The rule exists because broad nested catches hide degraded fallback behavior inside a larger exception-handling region. That makes the happy path, fallback path, and outer recovery path harder to scan together.

## What It Reports

Reports:

```csharp
try {
    if (reader.FieldCount == 1) {
        try {
            data = JsonSerializer.Deserialize<JsonElement>(jsonString);
        }
        catch {
            data = ToJsonElement(rawData);
        }
    }
}
finally {
    _connectionSemaphore.Release();
}
```

```csharp
try {
    foreach (var priceId in priceIds) {
        try {
            await ValidateAsync(priceId);
        }
        catch {
            invalidMappings.Add(priceId);
        }
    }
}
catch (Exception ex) {
    _logger.Error(ex, "Validation failed.");
}
```

## What It Does Not Report

Does not report:

```csharp
try {
}
finally {
    try {
        MetricsRecorder.RecordHttpRequest(method, endpoint, statusCode, elapsed);
    }
    catch (Exception ex) {
        _logger.LogDebug(ex, "Metrics recording failed.");
    }
}
```

```csharp
try {
    try {
        return int.Parse(value);
    }
    catch {
        return 0;
    }
}
catch (Exception ex) {
    return -1;
}
```

```csharp
try {
    try {
        _ = int.Parse(value);
    }
    catch (FormatException) {
        _ = 0;
    }
}
catch (Exception ex) {
}
```

```csharp
try {
    Action parseValue = () => {
        try {
            _ = int.Parse(value);
        }
        catch {
            _ = 0;
        }
    };

    parseValue();
}
catch (Exception ex) {
}
```

## Examples

Incorrect:

```csharp
try {
    try {
        return await primaryClient.ReadAsync(cancellationToken);
    }
    catch (Exception) {
        return await fallbackClient.ReadAsync(cancellationToken);
    }
}
catch (Exception ex) {
    return Failure(ex);
}
```

Correct:

```csharp
try {
    return await ReadWithFallbackAsync(cancellationToken);
}
catch (Exception ex) {
    return Failure(ex);
}
```

### Fallback State Mutation

Incorrect:

```csharp
try {
    foreach (var tenantId in tenantIds) {
        try {
            await SyncTenantAsync(tenantId, cancellationToken);
        }
        catch (Exception ex) {
            failedTenants.Add((tenantId, ex.Message));
        }
    }
}
catch (Exception ex) {
    _logger.Error(ex, "Tenant sync failed.");
}
```

Correct:

```csharp
try {
    foreach (var tenantId in tenantIds) {
        var result = await TrySyncTenantAsync(tenantId, cancellationToken);
        if (!result.Succeeded) {
            failedTenants.Add((tenantId, result.ErrorMessage));
        }
    }
}
catch (Exception ex) {
    _logger.Error(ex, "Tenant sync failed.");
}
```

### Extracted Parser Boundary

Incorrect:

```csharp
try {
    try {
        metadata = JsonSerializer.Deserialize<MetadataDto>(json);
    }
    catch {
        metadata = MetadataDto.Empty;
    }

    await SaveAsync(metadata, cancellationToken);
}
catch (Exception ex) {
    return Failure(ex);
}
```

Correct:

```csharp
try {
    var metadata = ParseMetadataOrDefault(json);
    await SaveAsync(metadata, cancellationToken);
}
catch (Exception ex) {
    return Failure(ex);
}
```

## Current Rollout

- Live warning scope: `apps/backend/Meridian.API` (current inventory: `0`)
- Default validation command: `rtk pnpm backend:analyzers:validate`
- Broader inventory command: `rtk pnpm backend:analyzers:inventory -- --diagnostics MER0002 --project apps/backend/Meridian.Analytics/Meridian.Analytics.csproj`
- Fallback loading check: `rtk pnpm backend:analyzers:validate:build`

`MER0002` remains enabled for `Meridian.API` warning enforcement while broader candidate review remains inventory-driven for other backend projects.

The current queue was reduced from the earlier `16`-candidate API scan to `10` after exempting nested broad catches that only log, and then to `0` in `Meridian.API` after targeted refactors.

Current replayable inventories:

- `Meridian.Analytics`: `1` candidate
- `Meridian.API`: `0` candidates

Candidate review artifact (historical API queue that has now been remediated):

- [MER0002 API candidate classification](docs/analysis/2026-04/2026-04-29-mer0002-api-candidate-classification.md)

Current known signal example:

- `apps/backend/Meridian.Analytics/Services/ClickHouseAnalyticsService.cs`

Current known non-candidate checks that stay quiet under the existing rule:

- `apps/backend/Meridian.API/Infrastructure/Observability/RequestMetricsMiddleware.cs`
- `apps/backend/Meridian.API/Infrastructure/Startup/StartupPipelineConfigurator.cs`
- `apps/backend/Meridian.API/Features/Billing/Services/Handlers/CheckoutSessionCompletedHandler.cs`
- `apps/backend/Meridian.API/Features/Reports/Services/EpcReportSectionMapper.cs`

See the classification artifact for the historical rollout-candidate, likely-exemption, and ambiguous buckets instead of treating the rule page as the source of queue truth.

## Preferred Refactor

1. Move the inner risky operation into a helper that owns its own exception boundary.
2. Return an explicit success/failure result instead of mutating fallback state inline.
3. Keep the outer `try` focused on one failure boundary.

## Edge Cases

- A nested `try/finally` without an inner catch does not report.
- A nested catch in an outer `finally` block does not report.
- A nested catch that returns or throws from the catch body does not report.
- Specific exception catches such as `catch (FormatException)` do not report.
- A nested catch that only logs does not report.
- A nested catch that logs and also mutates fallback state still reports.
- A nested catch inside a local function or lambda declared in the outer try block does not report.
- Generated code does not report.

## Suppression Guidance

Default stance: refactor first.

A suppression is only justified when the nested broad catch is the clearest local containment boundary and extracting it would materially obscure the behavior.
