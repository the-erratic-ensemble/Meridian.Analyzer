# MER0014

## Summary

`MER0014` flags obvious DTO/entity/model ownership drift.

## Intent

Model placement controls coupling. HTTP contracts should stay feature-local, persistence entities should stay in dedicated entity folders, and Core models should avoid EF persistence attributes.

## What It Reports

Reports:

- `*Entity` classes outside `Database/Entities` or migrations
- `*Request`, `*Response`, `*Dto`, or `*DTO` classes in `Core` or `Shared`
- EF persistence attributes on `Core` classes

## Examples

Incorrect:

```csharp
namespace Shared.Reports;

public sealed class ReportSearchResponseDto {
    public string Title { get; init; } = string.Empty;
}
```

Correct:

```csharp
namespace Api.Features.Reports.Models;

public sealed class ReportSearchResponseDto {
    public string Title { get; init; } = string.Empty;
}
```

### Entity Placement

Incorrect:

```csharp
namespace Api.Features.Reports.Models;

public sealed class ReportJobEntity {
    public Guid Id { get; set; }
}
```

Correct:

```csharp
namespace Shared.Database.Entities.Reports;

public sealed class ReportJobEntity {
    public Guid Id { get; set; }
}
```

### Persistence Attribute in Core

Incorrect:

```csharp
namespace Core.Reports;

[Table("reports")]
public sealed class ReportDefinition {
    public Guid Id { get; init; }
}
```

Correct:

```csharp
namespace Core.Reports;

public sealed class ReportDefinition {
    public Guid Id { get; init; }
}
```

## Preferred Refactor

Move HTTP contracts to feature-local API model folders, keep entities in dedicated entity folders, and keep Core persistence-agnostic.
